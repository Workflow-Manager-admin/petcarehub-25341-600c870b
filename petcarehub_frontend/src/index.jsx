import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

// PUBLIC_INTERFACE
// Entry for React app (PetCareHub)
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
